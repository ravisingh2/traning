import { render, screen, waitForElementToBeRemoved } from '@testing-library/react';
import App from './App';
import userEvent from '@testing-library/user-event';
  test('Login component testing', () => {    
    /*const {debug} = render(<App />);
    const username = screen.getByTitle("username")
    const password = screen.getByTitle("password")
    userEvent.type(username, "Ravi");
    userEvent.type(password, "1234")
    const loginBtn = screen.getByRole("button")
    userEvent.click(loginBtn);
    await waitForElementToBeRemoved(username);
    const accountTxt = await screen.findByText(/Account Number/i);
    
    expect(accountTxt).toBeInTheDocument();*/
  });
 

